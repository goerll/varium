# Next.js Example

The primary demo currently lives in [`../vite-react`](../vite-react/README.md).

This directory is reserved for the Next.js example described in the roadmap so the repo structure already matches the intended public layout.
